# Portainer Repository deployment reads substitutions from this file.
# Copy/rename to: stack.env (commit to your repo)

# Security
ADMIN_TOKEN=change_me_to_a_strong_random

# API
API_PORT=8080
RETENTION_DAYS=7
WORKER_NAME=worker-1
WORKER_VERSION=v0.1.0

# Postgres
POSTGRES_USER=hook
POSTGRES_PASSWORD=hook
POSTGRES_DB=hook
PG_PORT=5432

# Compose URLs (you can leave defaults)
DATABASE_URL=postgres://hook:hook@postgres:5432/hook?sslmode=disable
REDIS_URL=redis://redis:6379/0

# If you use the NPM shared network overlay, create the network first in Portainer:
# Networks -> Add -> Name: npm_proxy

